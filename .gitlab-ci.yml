build:
    tags:
        - java11
    script:
        - 'echo one'
        - 'mvn -version'
        - 'echo two'
        - 'java -version'
        - 'echo before maven'
        - 'mvn package'
        - 'echo done maven'
        - 'run-jlink.bat'
    artifacts:
        paths:
            - target/jlink
